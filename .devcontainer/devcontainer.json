{
  "name": "ghostty-web",
  "image": "nixpkgs/nix@sha256:79397901292ed23c3502177552e4d261c2d711720185e2b3a5beb715f35f595b",
  "postCreateCommand": "mkdir -p /etc/nix && echo 'experimental-features = nix-command flakes' > /etc/nix/nix.conf && git submodule update --init --recursive && nix develop --command echo 'Nix store ready'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ziglang.vscode-zig",
        "biomejs.biome"
      ]
    }
  }
}
